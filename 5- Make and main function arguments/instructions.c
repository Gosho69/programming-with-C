#include <stdio.h>

int main(void){
    printf("\n strlength - finds the length of a string");
    printf("\n strconcat - cocatenates two strings");
    printf("\n strcompare - compares two strings, returns -1 if the first string is before the second, 1 if the second string is before the first, and 0 if they are the same");
    return 0;
}